//take in every field submitted, scrutinize,
// correct and return proper errors if needed.

const itemFormValidator = () => {
    console.log("this is itemformvalidator");
}

export default itemFormValidator;